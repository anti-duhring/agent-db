---
phase: 02-runner-postgres-baseline
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - internal/benchmark/scenarios/append.go
  - internal/benchmark/scenarios/window.go
  - internal/benchmark/scenarios/list.go
  - internal/benchmark/scenarios/coldstart.go
  - internal/benchmark/scenarios/concurrent.go
  - go.mod
  - go.sum
autonomous: true
requirements: [SCEN-01, SCEN-02, SCEN-03, SCEN-04, SCEN-05]

must_haves:
  truths:
    - "AppendMessage scenario inserts a single message and measures write latency"
    - "LoadSlidingWindow scenario fetches last 20 messages from a conversation with 200+ messages"
    - "ListConversations scenario lists all conversations for a (partner_id, user_id) pair"
    - "ColdStartLoad scenario reads a sliding window without prior warmup on the connection"
    - "ConcurrentWrites scenario runs N goroutines appending messages in parallel with per-goroutine histograms"
  artifacts:
    - path: "internal/benchmark/scenarios/append.go"
      provides: "SCEN-01 AppendMessage scenario"
      contains: "type AppendScenario struct"
    - path: "internal/benchmark/scenarios/window.go"
      provides: "SCEN-02 LoadSlidingWindow scenario"
      contains: "type WindowScenario struct"
    - path: "internal/benchmark/scenarios/list.go"
      provides: "SCEN-03 ListConversations scenario"
      contains: "type ListScenario struct"
    - path: "internal/benchmark/scenarios/coldstart.go"
      provides: "SCEN-04 ColdStartLoad scenario"
      contains: "type ColdStartScenario struct"
    - path: "internal/benchmark/scenarios/concurrent.go"
      provides: "SCEN-05 ConcurrentWrites scenario"
      contains: "type ConcurrentScenario struct"
  key_links:
    - from: "internal/benchmark/scenarios/*.go"
      to: "internal/benchmark/scenario.go"
      via: "each scenario implements Scenario interface"
      pattern: "var _ benchmark\\.Scenario"
    - from: "internal/benchmark/scenarios/concurrent.go"
      to: "golang.org/x/sync/errgroup"
      via: "errgroup.WithContext for goroutine management"
      pattern: "errgroup\\.WithContext"
---

<objective>
Implement all five benchmark scenarios as concrete types satisfying the Scenario interface, each measuring a distinct database access pattern.

Purpose: These scenarios are the actual measurements the benchmark produces. Each targets a specific access pattern (write, read window, list, cold start, concurrent writes) to reveal Postgres performance characteristics.
Output: Five scenario files in internal/benchmark/scenarios/, each implementing the Scenario interface.
</objective>

<execution_context>
@/home/anti-duhring/alt/pocs/agent-db/.claude/get-shit-done/workflows/execute-plan.md
@/home/anti-duhring/alt/pocs/agent-db/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-runner-postgres-baseline/02-01-SUMMARY.md
@.planning/phases/02-runner-postgres-baseline/02-02-SUMMARY.md

@internal/repository/repository.go
@internal/domain/types.go
@internal/generator/generator.go

<interfaces>
<!-- Scenario interface and SeedResult from Plan 02 -->
<!-- These will exist after Plan 02-02 executes -->

From internal/benchmark/scenario.go (created by Plan 02-02):
```go
type Scenario interface {
    Name() string
    Setup(ctx context.Context, repo repository.ChatRepository, seed SeedResult) error
    Run(ctx context.Context, repo repository.ChatRepository) error
    Teardown(ctx context.Context) error
}
```

From internal/benchmark/runner.go (created by Plan 02-02):
```go
type SeedResult struct {
    PartnerID     uuid.UUID
    UserID        uuid.UUID
    Conversations []domain.Conversation
    OriginalData  generator.GeneratedData
}
```

From internal/repository/repository.go:
```go
type ChatRepository interface {
    CreateConversation(ctx context.Context, partnerID, userID uuid.UUID) (domain.Conversation, error)
    AppendMessage(ctx context.Context, conversationID uuid.UUID, role domain.Role, content string) (domain.Message, error)
    LoadWindow(ctx context.Context, conversationID uuid.UUID, n int) ([]domain.Message, error)
    ListConversations(ctx context.Context, partnerID, userID uuid.UUID) ([]domain.Conversation, error)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AppendMessage, LoadSlidingWindow, and ListConversations scenarios</name>
  <files>
    internal/benchmark/scenarios/append.go,
    internal/benchmark/scenarios/window.go,
    internal/benchmark/scenarios/list.go
  </files>
  <read_first>
    internal/benchmark/scenario.go,
    internal/benchmark/runner.go,
    internal/benchmark/results.go,
    internal/repository/repository.go,
    internal/domain/types.go
  </read_first>
  <action>
    1. Create `internal/benchmark/scenarios/append.go` (SCEN-01):
       - Package `scenarios`
       - `type AppendScenario struct` with fields: `convID uuid.UUID`, `msgIndex int`
       - Compile-time check: `var _ benchmark.Scenario = (*AppendScenario)(nil)`
       - `Name()` returns `"AppendMessage"`
       - `Setup(ctx, repo, seed)`: pick the first conversation from `seed.Conversations`, store its ID in `s.convID`. This is the conversation all appends target.
       - `Run(ctx, repo)`: call `repo.AppendMessage(ctx, s.convID, domain.RoleUser, "benchmark message")`. Increment `s.msgIndex` for varied content if desired, but the key measurement is the AppendMessage call latency.
       - `Teardown(ctx)`: no-op, return nil
       - Constructor: `func NewAppendScenario() *AppendScenario` returning zero-value struct

    2. Create `internal/benchmark/scenarios/window.go` (SCEN-02):
       - Package `scenarios`
       - `type WindowScenario struct` with field: `convID uuid.UUID`
       - Compile-time check: `var _ benchmark.Scenario = (*WindowScenario)(nil)`
       - `Name()` returns `"LoadSlidingWindow"`
       - `Setup(ctx, repo, seed)`: find a conversation from `seed.Conversations` that has 200+ messages in `seed.OriginalData.Messages[conv.ID]`. If the profile is "small" (10 messages), use the first conversation regardless (window will return all messages). Store `convID`.
       - `Run(ctx, repo)`: call `repo.LoadWindow(ctx, s.convID, 20)` — fetch last 20 messages
       - `Teardown(ctx)`: no-op
       - Constructor: `func NewWindowScenario() *WindowScenario`

    3. Create `internal/benchmark/scenarios/list.go` (SCEN-03):
       - Package `scenarios`
       - `type ListScenario struct` with fields: `partnerID, userID uuid.UUID`
       - Compile-time check: `var _ benchmark.Scenario = (*ListScenario)(nil)`
       - `Name()` returns `"ListConversations"`
       - `Setup(ctx, repo, seed)`: store `seed.PartnerID` and `seed.UserID`
       - `Run(ctx, repo)`: call `repo.ListConversations(ctx, s.partnerID, s.userID)`
       - `Teardown(ctx)`: no-op
       - Constructor: `func NewListScenario() *ListScenario`
  </action>
  <verify>
    <automated>cd /home/anti-duhring/alt/pocs/agent-db && go vet ./internal/benchmark/... && go build ./internal/benchmark/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/benchmark/scenarios/append.go contains `type AppendScenario struct`
    - internal/benchmark/scenarios/append.go contains `var _ benchmark.Scenario = (*AppendScenario)(nil)`
    - internal/benchmark/scenarios/append.go contains `func (s *AppendScenario) Run(`
    - internal/benchmark/scenarios/append.go contains `repo.AppendMessage(`
    - internal/benchmark/scenarios/window.go contains `type WindowScenario struct`
    - internal/benchmark/scenarios/window.go contains `repo.LoadWindow(ctx, s.convID, 20)`
    - internal/benchmark/scenarios/list.go contains `type ListScenario struct`
    - internal/benchmark/scenarios/list.go contains `repo.ListConversations(`
    - `go build ./internal/benchmark/...` exits 0
  </acceptance_criteria>
  <done>
    AppendMessage, LoadSlidingWindow, and ListConversations scenarios compile and implement the Scenario interface. Each targets a specific ChatRepository method for latency measurement.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ColdStartLoad and ConcurrentWrites scenarios</name>
  <files>
    internal/benchmark/scenarios/coldstart.go,
    internal/benchmark/scenarios/concurrent.go,
    go.mod, go.sum
  </files>
  <read_first>
    internal/benchmark/scenario.go,
    internal/benchmark/runner.go,
    internal/benchmark/scenarios/append.go,
    internal/benchmark/scenarios/window.go,
    internal/repository/repository.go
  </read_first>
  <action>
    1. Add errgroup dependency:
       ```bash
       go get golang.org/x/sync@v0.20.0
       ```

    2. Create `internal/benchmark/scenarios/coldstart.go` (SCEN-04):
       - Package `scenarios`
       - `type ColdStartScenario struct` with field: `convID uuid.UUID`
       - Compile-time check: `var _ benchmark.Scenario = (*ColdStartScenario)(nil)`
       - `Name()` returns `"ColdStartLoad"`
       - `Setup(ctx, repo, seed)`: find a conversation with 200+ messages (same logic as WindowScenario). Store `convID`.
       - `Run(ctx, repo)`: call `repo.LoadWindow(ctx, s.convID, 20)`
         - NOTE: The "cold start" aspect is controlled by the runner — SCEN-04 skips warmup iterations. The runner should NOT run warmup for this scenario. To signal this, add a method `SkipWarmup() bool` to the Scenario interface (or use a separate interface like `WarmupSkipper`). Simplest approach: add an optional interface:
           ```go
           // In scenario.go:
           type WarmupSkipper interface {
               SkipWarmup() bool
           }
           ```
         - ColdStartScenario implements `SkipWarmup() bool { return true }`
         - Update Runner.Run to check: `if ws, ok := sc.(WarmupSkipper); ok && ws.SkipWarmup() { /* skip warmup loop */ }`
       - `Teardown(ctx)`: no-op
       - Constructor: `func NewColdStartScenario() *ColdStartScenario`

    3. Create `internal/benchmark/scenarios/concurrent.go` (SCEN-05):
       - Package `scenarios`
       - Import `golang.org/x/sync/errgroup`
       - Import `hdrhistogram "github.com/HdrHistogram/hdrhistogram-go"`
       - `type ConcurrentScenario struct` with fields: `convID uuid.UUID`, `concurrency int`
       - Compile-time check: `var _ benchmark.Scenario = (*ConcurrentScenario)(nil)`
       - `Name()` returns `"ConcurrentWrites"`
       - `Setup(ctx, repo, seed)`: store first conversation ID from `seed.Conversations`
       - This scenario is SPECIAL — it manages its own measurement because multiple goroutines write concurrently. The Run method is called once per "iteration" by the runner, but each Run internally spawns N goroutines. Alternatively, the scenario can do a single iteration of concurrent work per Run call.
       - `Run(ctx, repo)`:
         - Create `errgroup.WithContext(ctx)` with `g.SetLimit(s.concurrency)` per D-09
         - Each goroutine calls `repo.AppendMessage(ctx, s.convID, domain.RoleUser, "concurrent benchmark message")`
         - Wait for all goroutines to complete
         - Return first error if any
       - The runner's outer loop handles timing — each `Run()` call is one "iteration" that does N concurrent writes. The HdrHistogram records the total time for all N concurrent writes to complete.
       - `Teardown(ctx)`: no-op
       - Constructor: `func NewConcurrentScenario(concurrency int) *ConcurrentScenario`

    4. Update `internal/benchmark/scenario.go` to add the optional `WarmupSkipper` interface:
       ```go
       // WarmupSkipper is an optional interface. Scenarios that implement it
       // with SkipWarmup() returning true will not have warmup iterations run.
       type WarmupSkipper interface {
           SkipWarmup() bool
       }
       ```

    5. Update `internal/benchmark/runner.go` to check for WarmupSkipper before the warmup loop:
       - Before the warmup `for` loop, add:
         ```go
         skipWarmup := false
         if ws, ok := sc.(WarmupSkipper); ok {
             skipWarmup = ws.SkipWarmup()
         }
         if !skipWarmup {
             for i := 0; i < r.config.Warmup; i++ {
                 _ = sc.Run(ctx, r.repo)
             }
         }
         ```
  </action>
  <verify>
    <automated>cd /home/anti-duhring/alt/pocs/agent-db && go vet ./internal/benchmark/... && go build ./internal/benchmark/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/benchmark/scenarios/coldstart.go contains `type ColdStartScenario struct`
    - internal/benchmark/scenarios/coldstart.go contains `func (s *ColdStartScenario) SkipWarmup() bool`
    - internal/benchmark/scenarios/coldstart.go contains `repo.LoadWindow(`
    - internal/benchmark/scenarios/concurrent.go contains `type ConcurrentScenario struct`
    - internal/benchmark/scenarios/concurrent.go contains `errgroup.WithContext`
    - internal/benchmark/scenarios/concurrent.go contains `g.SetLimit(s.concurrency)`
    - internal/benchmark/scenarios/concurrent.go contains `repo.AppendMessage(`
    - internal/benchmark/scenario.go contains `type WarmupSkipper interface`
    - internal/benchmark/runner.go contains `WarmupSkipper`
    - go.mod contains `golang.org/x/sync`
    - `go build ./internal/benchmark/...` exits 0
  </acceptance_criteria>
  <done>
    ColdStartLoad scenario skips warmup and measures first-read latency. ConcurrentWrites scenario spawns N goroutines via errgroup for parallel AppendMessage calls. Runner respects WarmupSkipper interface. All five scenarios compile against the Scenario interface.
  </done>
</task>

</tasks>

<verification>
- `go vet ./internal/benchmark/...` — no warnings
- `go build ./internal/benchmark/...` — all scenarios compile
- `go build ./...` — entire project compiles
- Each scenario file contains a compile-time interface check
</verification>

<success_criteria>
- All five scenarios implement the Scenario interface
- AppendMessage (SCEN-01) calls repo.AppendMessage on a seeded conversation
- LoadSlidingWindow (SCEN-02) calls repo.LoadWindow with n=20 on a conversation with 200+ messages
- ListConversations (SCEN-03) calls repo.ListConversations with seeded partner/user IDs
- ColdStartLoad (SCEN-04) implements WarmupSkipper to skip warmup iterations
- ConcurrentWrites (SCEN-05) uses errgroup with SetLimit(concurrency) for N parallel AppendMessage calls
</success_criteria>

<output>
After completion, create `.planning/phases/02-runner-postgres-baseline/02-03-SUMMARY.md`
</output>
