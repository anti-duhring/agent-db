---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - main.go
  - internal/domain/types.go
  - internal/repository/repository.go
autonomous: true
requirements:
  - IFACE-01
  - IFACE-02

must_haves:
  truths:
    - "ChatRepository interface compiles and is importable by adapter packages"
    - "Conversation and Message domain types exist with all specified fields"
    - "No database-specific types leak into the interface or domain packages"
    - "Role type uses typed string constants for user and assistant"
  artifacts:
    - path: "internal/domain/types.go"
      provides: "Conversation, Message, Role domain types"
      contains: "type Conversation struct"
    - path: "internal/repository/repository.go"
      provides: "ChatRepository interface"
      contains: "type ChatRepository interface"
    - path: "go.mod"
      provides: "Go module definition"
      contains: "module github.com/anti-duhring/agent-db"
    - path: "main.go"
      provides: "Entry point placeholder"
      contains: "package main"
  key_links:
    - from: "internal/repository/repository.go"
      to: "internal/domain/types.go"
      via: "import of domain package for Conversation, Message, Role types"
      pattern: "github.com/anti-duhring/agent-db/internal/domain"
---

<objective>
Initialize the Go module and define the foundational types and interface that every subsequent plan depends on.

Purpose: Establish the stable contracts (ChatRepository interface and domain types) that all adapter implementations and the generator will build against. This is the hard gate — nothing else compiles without these.
Output: Compilable Go module with domain types, ChatRepository interface, and minimal main.go.
</objective>

<execution_context>
@/home/anti-duhring/alt/pocs/agent-db/.claude/get-shit-done/workflows/execute-plan.md
@/home/anti-duhring/alt/pocs/agent-db/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module with domain types</name>
  <files>go.mod, go.sum, internal/domain/types.go</files>
  <read_first>
    - CLAUDE.md (technology stack, driver versions, module path)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-05 through D-08)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Typed Domain Types)
  </read_first>
  <action>
    1. Initialize Go module (per D-13):
       ```bash
       go mod init github.com/anti-duhring/agent-db
       ```
       Set `go 1.26` directive in go.mod per CLAUDE.md. Note: dev machine has Go 1.25.4 — avoid any 1.26-specific language features.

    2. Install uuid dependency (per D-05):
       ```bash
       go get github.com/google/uuid@v1.6.0
       ```

    3. Create `internal/domain/types.go` with package `domain` containing:

       - `type Role string` with constants (per D-07):
         - `const RoleUser Role = "user"`
         - `const RoleAssistant Role = "assistant"`

       - `type Conversation struct` with fields (per IFACE-02, D-05, D-08):
         - `ID uuid.UUID`
         - `PartnerID uuid.UUID`
         - `UserID uuid.UUID`
         - `CreatedAt time.Time`
         - `UpdatedAt time.Time`

       - `type Message struct` with fields (per IFACE-02, D-05, D-06, D-07, D-08):
         - `ID uuid.UUID`
         - `ConversationID uuid.UUID`
         - `Role Role` (not `domain.Role` — same package)
         - `Content string`
         - `TokenCount int` (per D-06, standard Go int)
         - `CreatedAt time.Time`

       Imports: `"time"` and `"github.com/google/uuid"`. No other imports. No database-specific types.
  </action>
  <verify>
    <automated>cd /home/anti-duhring/alt/pocs/agent-db && go build ./internal/domain/</automated>
  </verify>
  <acceptance_criteria>
    - go.mod contains `module github.com/anti-duhring/agent-db`
    - go.mod contains `go 1.26`
    - go.mod contains `github.com/google/uuid v1.6.0`
    - internal/domain/types.go contains `type Role string`
    - internal/domain/types.go contains `RoleUser Role = "user"`
    - internal/domain/types.go contains `RoleAssistant Role = "assistant"`
    - internal/domain/types.go contains `type Conversation struct`
    - internal/domain/types.go contains `type Message struct`
    - internal/domain/types.go contains `ID uuid.UUID`
    - internal/domain/types.go contains `TokenCount int`
    - internal/domain/types.go does NOT contain `database/sql` or any driver import
    - `go build ./internal/domain/` exits 0
  </acceptance_criteria>
  <done>Domain types compile. Conversation has 5 fields (ID, PartnerID, UserID, CreatedAt, UpdatedAt). Message has 6 fields (ID, ConversationID, Role, Content, TokenCount, CreatedAt). Role has two constants. No database imports.</done>
</task>

<task type="auto">
  <name>Task 2: Define ChatRepository interface and main.go</name>
  <files>internal/repository/repository.go, main.go</files>
  <read_first>
    - internal/domain/types.go (the types just created — verify exact field names and type names)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-01, D-02, D-03)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: ChatRepository Interface with Context)
  </read_first>
  <action>
    1. Create `internal/repository/repository.go` with package `repository` containing:

       `type ChatRepository interface` with these exact methods (per D-01, D-02, D-03):

       ```go
       CreateConversation(ctx context.Context, partnerID, userID uuid.UUID) (domain.Conversation, error)
       AppendMessage(ctx context.Context, conversationID uuid.UUID, role domain.Role, content string) (domain.Message, error)
       LoadWindow(ctx context.Context, conversationID uuid.UUID, n int) ([]domain.Message, error)
       ListConversations(ctx context.Context, partnerID, userID uuid.UUID) ([]domain.Conversation, error)
       ```

       Imports: `"context"`, `"github.com/google/uuid"`, `"github.com/anti-duhring/agent-db/internal/domain"`.

       Key contract details per locked decisions:
       - Every method takes `context.Context` as first arg (D-01)
       - Every method returns `(result, error)` (D-01)
       - `LoadWindow` returns `[]domain.Message` not a paginated struct (D-02)
       - `ListConversations` returns `[]domain.Conversation` sorted by last activity, no pagination (D-03)

    2. Create `main.go` in project root with package `main`:
       - A minimal `func main()` that prints a placeholder message like `fmt.Println("agent-db benchmark harness")`.
       - Import `"fmt"` only. This is a Phase 1 placeholder — CLI flags come in Phase 2.

    3. Run `go vet ./...` to verify the entire module compiles cleanly.
  </action>
  <verify>
    <automated>cd /home/anti-duhring/alt/pocs/agent-db && go vet ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - internal/repository/repository.go contains `type ChatRepository interface`
    - internal/repository/repository.go contains `CreateConversation(ctx context.Context, partnerID, userID uuid.UUID) (domain.Conversation, error)`
    - internal/repository/repository.go contains `AppendMessage(ctx context.Context, conversationID uuid.UUID, role domain.Role, content string) (domain.Message, error)`
    - internal/repository/repository.go contains `LoadWindow(ctx context.Context, conversationID uuid.UUID, n int) ([]domain.Message, error)`
    - internal/repository/repository.go contains `ListConversations(ctx context.Context, partnerID, userID uuid.UUID) ([]domain.Conversation, error)`
    - internal/repository/repository.go imports `github.com/anti-duhring/agent-db/internal/domain`
    - main.go contains `package main`
    - main.go contains `func main()`
    - `go vet ./...` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>ChatRepository interface is defined with all 4 methods per IFACE-01. Interface imports domain types (not database types). main.go compiles. Entire module passes go vet and go build.</done>
</task>

</tasks>

<verification>
```bash
cd /home/anti-duhring/alt/pocs/agent-db
go vet ./...
go build ./...
```
Both commands exit 0. The module compiles with domain types and ChatRepository interface importable from any internal package.
</verification>

<success_criteria>
- Go module initialized with correct path and Go version
- Domain types (Conversation, Message, Role) defined with all required fields
- ChatRepository interface defined with 4 methods matching D-01/D-02/D-03 signatures
- No database-specific imports in domain or repository packages
- `go vet ./...` and `go build ./...` pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
